import { createRequire } from 'module';
// We'll just define the values manually if we can't find a library
// Let's just output the colors into standard hex
// 0.985 0.005 85
// a small node script
console.log("No built in library for oklch, I will just use standard nice warm hex colors");
